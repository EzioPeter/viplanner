# general cfg
image_flip:             True
max_depth:              15
domain:                 rgb

# ros topics
vizTopic:               /viz_path_rgb
imgTopic:               /depth_camera_front/color/image_raw/compressed
infoTopic:              /depth_camera_front/color/camera_info
goalTopic:              /mp_waypoint
pathTopic:              /path

# frame ids
robot_frame:            base
odom_frame:             odom

# visualization
viz_path_sem_topic:     /viz_path_rgb
